# Code側スキル正典化 指示書 v1

本指示書は、Claude Code 側の3スキル + 1ルールファイルを「正典化された設計
思想 v3」と整合させるための一括改修指示書である。1PRで4ファイル変更を行う。

- 出典: Claude AI チャットセッション (引き継ぎドキュメント v3 メイン + part2 + Phase 2-C 起草本文)
- 作成日: 2026-04-28
- 作業ブランチ: `feature/code_side_skills_canonization`

---

## 第1章: メタ情報と前提

### 1-1. タスク概要

Claude Code 側の3スキルおよび1ルールファイルを「正典化された設計思想 v3」と
整合させるための一括改修を行う。Claude AI 側で設計思想の整理（v2 → v3）が
完了し、その内容を Code 側のスキル・ルールに反映する。

本タスクは UI 実装やアプリ機能の追加ではなく、`.claude/skills/` および
`.claude/rules/` 配下のドキュメント整備のみを対象とする。
ソースコード（src/、api/ 等）への変更は一切発生しない。

### 1-2. 触る対象ファイル一覧

以下4ファイルを変更する。1PRにまとめてマージする。

| # | ファイルパス | 種別 | 変更規模 |
|---|---|---|---|
| 1 | `.claude/skills/molkky-ui-design/SKILL.md` | 追記+修正+置換 | 大（4新規 + 3修正） |
| 2 | `.claude/skills/molkky-spec-writer/SKILL.md` | 追記+拡張 | 中（1新規 + 1拡張） |
| 3 | `.claude/skills/molkky-code-review/SKILL.md` | 追記のみ | 中（第10章新設） |
| 4 | `.claude/rules/ui-design.md` | 全置換 | 小（ポインタ化） |

### 1-3. なぜこの変更が必要か

過去にサマリ版（`.claude/rules/ui-design.md`）と詳細版（SKILL.md）の二重メンテで
矛盾が生じた経緯がある。また、UI設計思想が v2（3画面ベースの優先度マトリクス）から
v3（7画面ベースの質感アイデンティティ + 三層ドメイン分類 + 正典化強度）へ進化した
ため、Code側のスキル群もこれに追従する必要がある。

具体的な改修ポイント:

- **molkky-ui-design**: v2世代の3画面記述（GameScreen/Stats/Settings）を v3 の7画面記述
  （GameScreen/Stats/SetupScreen/Settings/WindMonitor/試合結果画面/試合カード）に置換し、
  「機能とデザインの両立思想」「色の意味の重複化禁止」「三層ドメイン分類」を新規追加する。
- **molkky-spec-writer**: 指示書作成時に正典化強度・三層ドメイン分類をチェックする
  「指示書作成前のチェック」セクションを新規追加し、「よくある失敗パターンの回避」を
  9項目拡張する（既存3項目 + 新規9項目 = 12項目）。
- **molkky-code-review**: PR作成前に正典化強度に応じた追加検証を行う第10章を新設する。
- **.claude/rules/ui-design.md**: 詳細版（SKILL.md）への一元化を実施し、本ファイルは
  ポインタ化する。

### 1-4. 確度ステータス

本指示書の記述確度は以下の通り。

- **実ファイル確認済み**: 4ファイル全ての変更前原文（GitHub MCP 経由で確認済み）。
  各章の変更前文言はすべて実ファイルから取得した正確な原文を記載している。
- **記憶ベース・要検証**: なし。本指示書には記憶ベース記述は含まれない。

### 1-5. 本指示書の前提知識

Claude Code は以下のドキュメントを既知として作業する。

- `.claude/skills/molkky-spec-writer/SKILL.md`: 指示書の読み方・段階的分割原則
- `.claude/skills/molkky-code-review/SKILL.md`: コードレビューの基本原則
- `CLAUDE.md`: プロジェクト全体ガイド

ただし、本タスクは `.claude/skills/molkky-spec-writer/SKILL.md` 自体を変更対象に
含む（作業2）。**作業2 で変更されるのは Claude Code への指示書作成ガイドラインで
あり、Claude Code 自身の挙動（本指示書の読み方）には影響しない**。
本指示書は v1 時点の molkky-spec-writer スキルに準拠して書かれている。

---

## 第2章: 共通ルール・作業前準備

### 2-1. Claude Code 共通ルール

- 全コミットメッセージ・PR本文・コメント・対話レスポンスは **日本語** で書く
- 全変更は事前にビルド検証を通す（`npm run build`）
- 段階的ビルド検証は本指示書の第7章で指定するタイミングで実施する
- 各 Edit 操作は前後文脈付きで個別実行する。`replace_all` は使用しない
- 変更前文言は本指示書記載の原文と **1文字単位で完全一致** することを Edit 前に
  確認する。一致しない場合は Edit を実行せず、太一さんに確認を求める

### 2-2. 鉄則

- 本指示書に記載のない変更は加えない（ファイルの末尾改行追加、空行調整、
  既存記述のリファクタリング等の「ついでの修正」も含めて禁止）
- 本指示書記載の変更後文言を **1文字単位で完全一致** で実装する
- 各作業（作業1〜作業4）は **1コミット = 1ファイル** で分離する
- 太一さんがマージするまで PR は手動レビュー対象とする。Claude Code は
  自動マージを行わない

### 2-3. ブランチ名

```
feature/code_side_skills_canonization
```

### 2-4. 作業開始時の手順

以下の手順を作業開始前に必ず実行する。

```bash
# 1. main 最新化
git checkout main
git pull origin main

# 2. 基準SHA動的確認（記録のみ、指示書には固定値で書かない）
git rev-parse HEAD

# 3. 各変更対象ファイルの blob SHA を確認（変更前原文の同一性確認用）
git rev-parse main:.claude/skills/molkky-ui-design/SKILL.md
git rev-parse main:.claude/skills/molkky-spec-writer/SKILL.md
git rev-parse main:.claude/skills/molkky-code-review/SKILL.md
git rev-parse main:.claude/rules/ui-design.md

# 4. 作業ブランチ作成
git checkout -b feature/code_side_skills_canonization
```

確認した SHA は作業ログとして記録し、PR本文に記載する（基準SHA固定回避のため、
本指示書には固定値を記載していない）。

### 2-5. 計画→承認→実装フロー（必須）

本タスクは規模が大きいため、以下のフローを必ず守る。

1. **計画提示**: Claude Code は本指示書を読了後、以下を含む計画を太一さんに提示する
   - 各作業のサブタスク分解
   - 段階的ビルド検証の実施タイミング
   - 想定される疑問点・確認事項
   - 推定作業時間
2. **承認待ち**: 太一さんから明示的な承認を得るまで実装に着手しない
3. **実装着手**: 承認後、第3章から第6章の順で実装する

途中で本指示書の記述に不明点・矛盾点を発見した場合は、独自判断で進めず
太一さんに確認を求める。

### 2-6. 作業順序

第3章 → 第4章 → 第5章 → 第6章 の順で実装する（最大規模ファイルから着手）。
各作業の完了条件を満たしてから次の作業に進む。

---

## 第3章: 作業1 — molkky-ui-design/SKILL.md の改修

### 3-1. 変更対象ファイル情報

- パス: `.claude/skills/molkky-ui-design/SKILL.md`
- 確度: 実ファイル確認済み（GitHub MCP）
- 変更前ファイルサイズ: 約 7238 B
- 変更後ファイルサイズ: 約 13000〜14000 B
- 変更種別: 追記 + 修正 + 部分置換（複数 Edit 操作）

### 3-2. 現状構造（実ファイル確認済み）

現行 `molkky-ui-design/SKILL.md` は以下の主要セクションで構成されている。

1. 運用背景（このスキルが「運用ルールである」旨を述べる短い前文）
2. 画面別の設計方針（**v2世代の3画面記述: GameScreen / Stats / Settings**）
3. 情報ブロック
4. 視線誘導の原則
5. frontend-design プラグインとの関係（**簡素な記述のみ**）
6. AI生成っぽいUI回避チェックリスト
7. iPad改善の採用チェックリスト
8. デザイン品質の具体ルール
9. 人間の視点での最終検証

本作業では以下の変更を加える。

- **新規追加4セクション**: 機能とデザインの両立思想 / 色の意味の重複化禁止 /
  三層ドメイン分類 / 7画面ベースの画面別の設計方針（旧3画面記述を全面置換）
- **修正3セクション**: 運用背景 / frontend-design プラグインとの関係 /
  iPad改善の採用チェックリスト #1
- **維持5セクション**: 情報ブロック / 視線誘導の原則 / AI生成っぽいUI回避
  チェックリスト / デザイン品質の具体ルール / 人間の視点での最終検証

### 3-3. 改修方針（変更全7サブセクション）

| # | サブセクション | 種別 | 配置位置 |
|---|---|---|---|
| 1-A | 運用背景セクションの修正 | 修正 | 既存位置 |
| 1-B | 旧3画面記述の全面置換 → 新7画面記述 | 全面置換 | 既存位置 |
| 1-C | 「機能とデザインの両立思想」新規追加 | 新規 | 1-B の直前 |
| 1-D | 「色の意味の重複化禁止」新規追加 | 新規 | 「視線誘導の原則」の直後 |
| 1-E | 「frontend-design プラグインとの関係」全面置換 | 全面置換 | 既存位置 |
| 1-F | 「三層ドメイン分類」新規追加 | 新規 | 「デザイン品質の具体ルール」の直後 |
| 1-G | 「iPad改善の採用チェックリスト」#1 修正 | 修正 | 既存位置 |

### 3-4. 共通の Edit 実行方針（全サブセクションに適用）

各サブセクションの Edit 実行前に、以下の手順を **必ず** 踏むこと。

1. **GitHub MCP または `git show main:.claude/skills/molkky-ui-design/SKILL.md` で
   実ファイルから変更前文言を取得する**
2. 取得した原文と本指示書の「変更前文言」が **1文字単位で完全一致** することを確認
3. 一致した場合のみ `str_replace` で該当箇所を変更する
4. 一致しない場合は Edit を実行せず、太一さんに確認を求める

`replace_all` は使用しない。各 Edit は前後文脈を含めて一意化された個別 Edit と
すること。

### 3-5. 変更1-A: 運用背景セクションの修正

#### 変更概要

冒頭の「運用背景」セクションは、現状「frontend-design プラグインの方向性を
モルック固有制約で上書きする運用ルール」である旨を述べている。これを v3 世代の
表現に更新し、「機能とデザインの両立思想を中核とする」記述に置き換える。

#### 変更前文言

実ファイル確認済み。現状の運用背景セクション全文を Claude Code が GitHub MCP
または `git show main:.claude/skills/molkky-ui-design/SKILL.md` で取得し、
冒頭の運用背景セクション（「## 運用背景」見出しから次の「##」見出し直前まで）
を本指示書の「変更後文言」に置換する。

参考として、現状の運用背景セクションは概ね以下の趣旨である:

- このスキルは frontend-design プラグインの方向性をモルック固有の制約で
  上書きする運用ルールである
- 直射日光下の視認性・iPad三脚固定・屋外運用といった特殊環境を前提とする

#### 変更後文言

```markdown
## 運用背景

このスキルは、モルックスコアラー固有の UI 設計思想を Claude Code が
参照するための運用ルールである。frontend-design プラグインの一般的な
方向性を、モルック固有の制約・運用環境・正典化された設計判断で上書きする。

中核となる思想は **機能とデザインの両立** である。屋外運用の視認性
（iPad 三脚固定 / 直射日光下 / 試合中の短時間判断）と、デザイン品質
（情報階層・余白・色数制御・装飾抑制）を、どちらかを犠牲にせず両立させる
ことを目指す。一方を諦めた瞬間に AI生成っぽい UI に堕する。

本スキルは UI v3 世代の正典化を反映している。各画面は「正典化強度」と
「質感アイデンティティ」を持ち、強度に応じて改修時の根拠強度を変える。
詳細は本ドキュメントの「画面別の設計方針」セクションを参照。
```

#### Edit 実行方針

- 変更前文言が GitHub MCP で取得した実ファイルと完全一致することを確認
- `str_replace` で「## 運用背景」見出しから次の「##」見出し直前までを
  上記「変更後文言」で置換
- 「## 運用背景」見出し自体を残す形で本文のみを置換するか、見出しごと
  置換するかは Claude Code の判断に任せる（最終結果が「変更後文言」と
  一致すればよい）

### 3-6. 変更1-B: 旧3画面記述の全面置換 → 新7画面記述

#### 変更概要

現状の「画面別の設計方針」セクションは v2 世代の3画面記述（GameScreen /
Stats / Settings）で構成されている。これを v3 世代の **7画面記述** に
全面置換する。

新7画面: GameScreen / Stats / SetupScreen / Settings / WindMonitor /
試合結果画面 / 試合カード

#### 変更前文言

実ファイル確認済み。現状の「## 画面別の設計方針」セクション全文（見出し
からの直後の本文すべて、次の「##」見出し直前までの範囲）を Claude Code が
GitHub MCP または `git show main:.claude/skills/molkky-ui-design/SKILL.md`
で取得すること。

参考として、現状の画面別の設計方針セクションは概ね以下の構造である:

- 「### GameScreen」: 視認性最優先、操作性二次、美観三次の方針記述
- 「### Stats」: 美観最優先、情報量二次、操作性三次の方針記述
- 「### Settings」: 操作性最優先、わかりやすさ二次、美観三次の方針記述

このセクション全体を以下の「変更後文言」で全面置換する。

#### 変更後文言

```markdown
## 画面別の設計方針

各画面は「正典化強度」と「質感アイデンティティ」を持つ。改修時は
正典化強度に応じて根拠強度を変える。

正典化強度の3段階:

- **濃い**: 既に正典化済みの確定領域。改修にはエビデンス（実機検証 +
  ユーザー観察 + 既存実装が不十分である根拠）が必須
- **中強度**: 改修方針は確定済みだが、具体実装は柔軟に許容する領域。
  改修理由の明記が必須
- **薄い**: 通常運用領域。エビデンス不要、通常のレビュー基準で進める

### GameScreen（正典化強度: 濃い、ただし GameWindWidget は薄い）

質感アイデンティティ: **試合中の短時間判断を支える計器盤**。
チーム情報ブロック（チーム名 + プレイヤー名 + 点数 + ミス）が凝集単位。
直射日光下視認性は装備で解決済みのため第一優先ではないが、
対策リソースは一定残す。

設計上の留意点:

- 情報ブロックを分割配置せず、まとまりとして表示する
- チーム固有色は廃止し、アクティブ/非アクティブの2状態のみで区別
- 背景はセミライト、濃紺はヘッダー・アクティブ強調に限定
- 入力部ボタンサイズは現行 isTablet ロジックの上限を超えない
- GameWindWidget（pill型風速計）のみ正典化強度=薄い扱い。
  通常のレビュー基準で改修可能

### Stats（方針=中強度、具体実装=薄い）

質感アイデンティティ: **濃厚質感 × Strava/Whoop 的ストイックさ**。
分析画面としての密度感と、試合後の落ち着いた振り返り体験を両立する。

設計上の留意点:

- 分析性とデザイン性を共存させる
- データの密度感を表現しつつ、試合直後の高揚を残さない抑制された質感
- 具体実装の試行錯誤は許容(強度=薄い)

### SetupScreen（正典化強度: 中強度）

質感アイデンティティ: **計器盤のブランド刻印**。試合開始前の準備時間を、
プロフェッショナルな計器を起動する儀式として演出する。

設計上の留意点:

- 操作性を最優先しつつ、設定画面としての計器感を表現
- ブランド要素（タイトル・バージョン情報等）の刻印感を保つ

### Settings（正典化強度: 中強度）

質感アイデンティティ: **操作性 × デザイン**。設定変更の確実性と、
誤操作回避を両立する。

設計上の留意点:

- 操作性を最優先しつつデザイン性も維持
- 変更が即座に反映される項目と確認を要する項目の区別を明確化

### WindMonitor（正典化強度: 濃い）

質感アイデンティティ: **航海計器風ダークテーマ**。風向風速の精密表示を
専用画面として独立させ、計器としての完結性を持つ。

設計上の留意点:

- 航海計器の質感（ダークベース、計器盤の精度感）を保持
- 改修にはエビデンス必須。色の意味の重複化を絶対回避

### 試合結果画面（正典化強度: 中強度）

質感アイデンティティ: **Stats抑制版の濃厚質感**。試合直後の振り返りに
ふさわしい、Stats の濃厚さを少し抑えた抑制された表現。

設計上の留意点:

- Stats と同系統の質感だが、より抑制された表現
- 試合直後の感情の波を増幅しすぎない設計

### 試合カード（正典化強度: 中強度）

質感アイデンティティ: **親画面に同調する控えめな器**。Stats画面・
試合結果画面に配置されるカード単体は、親画面の質感を損なわない控えめな
表現にとどめる。

設計上の留意点:

- 親画面の質感アイデンティティを優先する
- カード単体で主張せず、リスト全体・親画面全体での視認体験を優先
```

#### Edit 実行方針

- 変更前文言が GitHub MCP で取得した実ファイルと完全一致することを確認
- `str_replace` で「## 画面別の設計方針」見出しから次の「##」見出し直前
  までを上記「変更後文言」で完全置換
- 旧3画面記述（GameScreen / Stats / Settings の v2 世代記述）が消失し、
  新7画面記述に置き換わることを確認

### 3-7. 変更1-C: 「機能とデザインの両立思想」新規追加

#### 変更概要

「画面別の設計方針」セクションの **直前** に、新規セクション「機能とデザインの
両立思想」を追加する。本セクションはモルックスコアラー UI 設計の中核思想を
明示するもので、以降のすべての画面別方針・チェックリストの前提となる。

#### 配置位置

- 配置先: 「## 画面別の設計方針」見出しの **直前**
- 既存の 1-A（運用背景）と 1-B（画面別の設計方針）の間に挟まる形になる

#### 変更前文言

なし（新規追加のため）。

ただし配置位置を一意化するため、Edit 実行時は以下のように **既存の見出し
「## 画面別の設計方針」を文脈アンカーとして使用** すること。

#### 変更後文言

```markdown
## 機能とデザインの両立思想

モルックスコアラーの UI 設計は、**機能とデザインのどちらかを犠牲にしない**
ことを中核思想とする。一方を諦めた瞬間に AI生成っぽい UI に堕する。

### 両立すべき2軸

- **機能軸**: 屋外運用の視認性・iPad 三脚固定下の操作性・試合中の短時間
  判断・誤操作回避
- **デザイン軸**: 情報階層の明瞭さ・余白と整列の品位・色数制御・装飾抑制・
  質感アイデンティティの一貫性

### 画面別の重み配分

すべての画面で両軸を満たすことを前提としつつ、画面の用途に応じて
**最も求められる要素 × 高いデザイン性の共存** を画面別方針として定める。

- GameScreen: **視認性 × デザイン**
- Stats: **分析性 × デザイン**
- Settings: **操作性 × デザイン**
- WindMonitor: **視認性 × デザイン**（航海計器風の濃い質感）
- SetupScreen: **操作性 × デザイン**（計器盤のブランド刻印）
- 試合結果画面: **振り返り体験 × デザイン**（Stats抑制版）
- 試合カード: **親画面同調 × デザイン**

各画面の質感アイデンティティの詳細は次セクション「画面別の設計方針」を参照。

### 両立を破る代表的アンチパターン

- 視認性のために色数を増やしすぎ、結果として AI生成っぽい UI に堕する
- デザインのために情報密度を下げすぎ、試合中の短時間判断ができなくなる
- 機能要件を理由に装飾過剰を許容し、情報階層が崩れる
- デザイン優先で機能色（警告色等）を弱め、誤操作リスクが上がる

これらは「どちらかを諦めた」結果であり、本スキルでは原則として許容しない。
```

#### Edit 実行方針

- `str_replace` の `old_str` に「## 画面別の設計方針」見出しの直前数行を含めて
  文脈を一意化する
- `new_str` に「上記の変更後文言 + 改行 + 既存の `## 画面別の設計方針` 見出し」を
  指定し、新規セクションが既存セクションの直前に挿入される形で実装する
- 既存の「## 画面別の設計方針」見出しの本文は **絶対に変更しない**
  （別途 1-B で全面置換するが、本サブセクション 1-C の Edit では触らない）

#### 完了確認

- 「## 機能とデザインの両立思想」が新規セクションとして存在する
- セクション内に「両立すべき2軸」「画面別の重み配分」「両立を破る代表的
  アンチパターン」の3サブセクションが存在する
- 配置位置が「## 運用背景」と「## 画面別の設計方針」の間である

### 3-8. 変更1-D: 「色の意味の重複化禁止」新規追加

#### 変更概要

「視線誘導の原則」セクションの **直後** に、新規セクション「色の意味の重複化
禁止」を追加する。本セクションは色の役割分離原則を v2 世代の絶対表現
（「色の役割は厳密に分離」）から v3 世代のニュアンス表現
（「重複化を禁止する、ただし正典化強度の濃い領域では絶対適用」）に進化させた
内容である。

#### 配置位置

- 配置先: 「## 視線誘導の原則」セクション本文の **直後**、次の見出しの直前

#### 変更前文言

なし（新規追加のため）。配置位置を一意化するため、Edit 実行時は「## 視線誘導の
原則」セクションの末尾近辺と次の見出し（既存構造では「## frontend-design
プラグインとの関係」）を文脈アンカーとして使用する。

#### 変更後文言

```markdown
## 色の意味の重複化禁止

色の使い分けは UI の判別性に直結する。1つの色に複数の意味を持たせると、
ユーザーは「この色が何を意味するか」を文脈ごとに判断する必要が生じ、
試合中の短時間判断が破綻する。

### 色の役割カテゴリ

- **ブランド色**: アプリのアイデンティティを示す色（例: 濃紺）
- **アクション色**: ユーザー操作のフィードバックを示す色
- **状態色**: アクティブ/非アクティブ、選択状態等を示す色
- **警告色**: 誤操作回避・注意喚起を示す色（例: 赤系）
- **機能色**: 特定の機能要素（例: 風速、距離等）を識別する色

これらのカテゴリ間で色を共有させない。

### 正典化強度との関係

色の役割分離は、画面の正典化強度に応じて適用厳格度が変わる。

- **濃い領域（GameScreen本体 / WindMonitor）**: 絶対適用。
  1色1意味の原則を破る実装は受け付けない
- **中強度領域**: 強く推奨。やむを得ず重複させる場合は改修理由を
  コミットメッセージに明記
- **薄い領域**: 推奨。実装の柔軟性を許容しつつ、レビュー時に確認

### 代表的な重複化アンチパターン

- アクション色と警告色を同じ赤系で表現し、ユーザーが「タップしてよい」のか
  「警告」なのか判断できない
- 状態色（アクティブ）とブランド色を同じ濃紺で表現し、どこがアクティブで
  どこが装飾的な濃紺かが識別できない
- 機能色（風速のレベル表示）が他の状態色と被り、風速計か状態表示かが
  読み取れない

これらは v3 世代では明示的に避ける。色の追加は「削る」発想で慎重に行う。

### 確認手順

- 新規実装・改修時、画面内で使われる色を列挙する
- 各色がどのカテゴリ（ブランド/アクション/状態/警告/機能）に属するかを明示
- 同じ色が2カテゴリ以上に属していないかを確認
- 属している場合、正典化強度を確認し、濃い領域では実装を見直す
```

#### Edit 実行方針

- `str_replace` の `old_str` に「## 視線誘導の原則」セクションの末尾数行と
  次の見出し「## frontend-design プラグインとの関係」の冒頭を含めて文脈を
  一意化する
- `new_str` に「視線誘導の原則の末尾 + 改行 + 上記の変更後文言 + 改行 +
  次の見出し冒頭」を指定し、新規セクションが既存2セクションの間に挿入される
  形で実装する
- 「## 視線誘導の原則」セクション本文・「## frontend-design プラグインとの
  関係」セクション本文は **絶対に変更しない**（後者は別途 1-E で全面置換するが、
  本サブセクション 1-D の Edit では触らない）

#### 完了確認

- 「## 色の意味の重複化禁止」が新規セクションとして存在する
- セクション内に「色の役割カテゴリ」「正典化強度との関係」「代表的な重複化
  アンチパターン」「確認手順」の4サブセクションが存在する
- 配置位置が「## 視線誘導の原則」と「## frontend-design プラグインとの関係」の
  間である

### 3-9. 変更1-E: 「frontend-design プラグインとの関係」全面置換

#### 変更概要

現状の「frontend-design プラグインとの関係」セクションは簡素な記述に留まる。
これを v3 世代の整理に基づき、**プラグイン提示物の採否判断ロジック** を
明確化した内容に全面置換する。

#### 変更前文言

実ファイル確認済み。現状の「## frontend-design プラグインとの関係」セクション
全文（見出しからの直後の本文すべて、次の「##」見出し直前までの範囲）を
Claude Code が GitHub MCP または `git show` で取得し、本指示書の「変更後文言」に
全面置換する。

参考として、現状のセクションは概ね「frontend-design プラグインの方向性を
モルック固有の制約で上書きする」旨を簡素に述べた記述である。

#### 変更後文言

```markdown
## frontend-design プラグインとの関係

frontend-design プラグインは Anthropic が提供する公式プラグインで、
一般的な Web UI 設計のベストプラクティスを Claude Code に提供する。
本スキル `molkky-ui-design` は、このプラグインの提示物を **モルック
スコアラー固有の制約で上書きする位置づけ** にある。

### 採否判断のロジック

frontend-design プラグインが提示するパターン・コンポーネント・スタイルが
モルックスコアラーで採用可能かを、以下の順で判定する。

#### Step 1: 機能とデザインの両立を破らないか

提示物が「機能軸」または「デザイン軸」のどちらかを犠牲にしていないかを
確認する。屋外運用の視認性・試合中の短時間判断・iPad 三脚固定の操作性
のいずれかを損なう提示は不採用。

#### Step 2: 該当画面の質感アイデンティティと整合するか

提示物が該当画面の質感アイデンティティ（GameScreen=計器盤、Stats=濃厚質感×
ストイック、WindMonitor=航海計器風、SetupScreen=ブランド刻印 等）と
整合するかを確認する。整合しない提示は不採用。

#### Step 3: 色の意味の重複化禁止に抵触しないか

提示物の配色が、画面内で既に役割を持つ色と重複していないかを確認する。
重複がある場合、特に正典化強度=濃い領域では即不採用。

#### Step 4: 正典化強度と改修根拠が釣り合うか

提示物が改修対象とする画面の正典化強度を確認する。

- 濃い領域: エビデンス（実機検証 + ユーザー観察 + 既存実装が不十分である
  根拠）が揃わない限り不採用
- 中強度領域: 改修理由が明確であれば採用
- 薄い領域: 通常レビュー基準で採用判断

### プラグイン提示物の良い使い方

- **アイデア源として参照する**: 「こういうパターンもある」という気付きを
  得る用途では積極的に使う
- **iPad 改善案の素材にする**: iPad 改善の採用チェックリストを通したうえで
  採否を決める
- **コンポーネント設計の出発点**: ゼロから設計するより、プラグイン提示を
  起点に「モルックでは何を変えるべきか」を考える方が早い

### プラグイン提示物の悪い使い方

- **そのまま採用する**: 「frontend-design がこう言ったから」だけでは
  採用根拠にならない
- **モルック固有制約を後回しにする**: 視認性・三脚固定・短時間判断を
  「あとで調整する」前提でプラグイン提示を採用するのは禁止
- **正典化強度の高い領域に無根拠で適用する**: 濃い領域への変更は
  プラグイン提示を採用根拠にできない

### 階層関係の明文化

- frontend-design プラグイン: 一般原則の提供
- `molkky-ui-design` スキル: モルック固有制約による上書き（本スキル）
- 個別 PR の改修判断: 上記2階層を踏まえた具体判断

判断が衝突した場合、**基本的には `molkky-ui-design` の記述を優先** する。

ただし以下の例外がある:

- 太一さんから明示的に「frontend-design プラグインの提示を試験的に
  導入する」旨の指示がある場合は、例外的にプラグイン提示の優先度を
  上げて採用検討する
- この例外運用は、外部のデザイン知見を取り込むための試験導入として
  位置づけられる。例外採用が指示される場合、太一さん側で導入根拠
  （どの提示物をなぜ取り込みたいか）が必ず明示される
- 例外採用された変更は、運用評価を経て本スキルへの正典化（中強度〜
  濃い領域への昇格 / 既存原則の改訂）の検討対象になる

Claude Code 自身の判断で frontend-design プラグインの優先度を上げる
ことは禁止。例外運用の起点は **必ず太一さんからの明示的指示** である
ことを徹底する。
```

#### Edit 実行方針

- 変更前文言が GitHub MCP で取得した実ファイルと完全一致することを確認
- `str_replace` で「## frontend-design プラグインとの関係」見出しから次の
  「##」見出し直前までを上記「変更後文言」で完全置換
- 旧記述（簡素版）が消失し、新記述（採否判断ロジック付き）に置き換わる

#### 完了確認

- 「## frontend-design プラグインとの関係」セクションの本文が新記述に
  置き換わっている
- セクション内に「採否判断のロジック（Step 1〜4）」「プラグイン提示物の
  良い使い方」「プラグイン提示物の悪い使い方」「階層関係の明文化」の
  4サブセクションが存在する
- セクションの位置（前後の見出しとの順序関係）は変更前と同じ

### 3-10. 変更1-F: 「三層ドメイン分類」新規追加

#### 変更概要

「デザイン品質の具体ルール」セクションの **直後** に、新規セクション「三層
ドメイン分類」を追加する。本セクションは UI 改修・機能追加要望が来た時に、
それが「触ってよい領域なのか / 触らない領域なのか」を一次判定するための
分類フレームワークである。

#### 配置位置

- 配置先: 「## デザイン品質の具体ルール」セクション本文の **直後**、次の
  見出しの直前
- 既存構造では「## デザイン品質の具体ルール」の次は「## 人間の視点での最終
  検証」となっている見込み

#### 変更前文言

なし（新規追加のため）。配置位置を一意化するため、Edit 実行時は「## デザイン
品質の具体ルール」セクションの末尾近辺と次の見出し（既存構造では「## 人間の
視点での最終検証」）を文脈アンカーとして使用する。

#### 変更後文言

```markdown
## 三層ドメイン分類

UI 改修・機能追加の要望が来た時、それが「現状実装の修正」「拡張仕様の受け入れ」
「領域外の不採用」のどれに該当するかを最初に判定する。判定を経ずに改修を
進めると、本来不採用にすべき要望が混入したり、既に正典化された領域が無根拠で
改変されるリスクがある。

### ドメイン1: 現状実装

既に正典化済みの確定領域を指す。具体的には正典化強度=濃い〜中強度の領域に
おける、現行実装そのもの。

#### 該当例

- GameScreen 本体の情報ブロック構造（チーム名+プレイヤー名+点数+ミス）
- WindMonitor の航海計器風ダークテーマ
- SetupScreen の計器盤ブランド刻印
- Stats の濃厚質感×ストイック方針
- 試合結果画面の Stats抑制版質感
- 試合カードの親画面同調設計

#### 改修方針

- 触らないのが基本
- 触る場合は正典化強度に応じた根拠強度が必要
  - 濃い領域: エビデンス（実機検証 + ユーザー観察 + 既存実装が不十分である
    根拠）必須
  - 中強度領域: 改修理由の明記必須
- 「ついでの修正」「リファクタリングの巻き添え」での改変は禁止

### ドメイン2: 拡張仕様

新機能の受け入れ枠。本アプリのスコープ内で、まだ実装されていないが将来的に
受け入れる可能性がある領域。

#### 該当例

- 新しい統計指標の追加（Stats 内）
- 新しい設定項目の追加（Settings 内）
- 新しい風速関連表示の追加（WindMonitor 内）
- マルチコート機能の拡張
- AI 分析機能の拡張（Phase 4 環境データ連携 等）

#### 改修方針

- 受け入れ可能
- 受け入れ時は `DESIGN.md` 該当章への追記で対応する
- 新機能は最初 **正典化強度=薄い** でスタートする
- 数試合運用して問題なければ強度を上げて確定する
- 確定後は「現状実装」ドメインに昇格する

### ドメイン3: 領域外

本アプリのスコープ外。基本的に不採用とすべき要望。

#### 該当例

- 他のスポーツへの汎用化（フィンランド発祥のモルック専用アプリである前提）
- SNS 連携の高度化（基本機能優先）
- ゲーム要素の追加（実用ツールとしての位置づけを優先）
- アプリ内通貨・課金システム
- 他アプリとの過剰な連携

#### 改修方針

- 基本不採用
- 太一さんから明示的に「領域外だが導入したい」指示がある場合のみ例外検討
- Claude Code 自身の判断で領域外の機能を追加することは禁止

### 判定フロー

要望が来た時、以下の順で判定する。

1. **ドメイン3（領域外）か?** → Yes なら基本不採用、太一さんに確認
2. **ドメイン2（拡張仕様）か?** → Yes なら DESIGN.md への追記要否を確認した
   うえで実装着手
3. **ドメイン1（現状実装）か?** → Yes なら正典化強度を確認し、必要な根拠
   強度を満たしてから実装着手

### 判定が難しいケース

- **ドメイン2 と 1 の境界**: 既存画面に新要素を追加する場合、既存要素を
  改変するか・既存に追加するだけかで判定が変わる
- **ドメイン3 と 2 の境界**: 「拡張」と「領域外」の判定は太一さんの判断に
  委ねる。Claude Code は判定が難しい場合、独自判断で進めず確認を求める

判定根拠は PR 本文に明記する（特にドメイン1 への変更時は正典化強度と
根拠強度を併記）。
```

#### Edit 実行方針

- `str_replace` の `old_str` に「## デザイン品質の具体ルール」セクションの
  末尾数行と次の見出し「## 人間の視点での最終検証」の冒頭を含めて文脈を
  一意化する
- `new_str` に「デザイン品質の具体ルールの末尾 + 改行 + 上記の変更後文言 +
  改行 + 次の見出し冒頭」を指定し、新規セクションが既存2セクションの間に
  挿入される形で実装する
- 「## デザイン品質の具体ルール」セクション本文・「## 人間の視点での最終
  検証」セクション本文は **絶対に変更しない**

#### 完了確認

- 「## 三層ドメイン分類」が新規セクションとして存在する
- セクション内に「ドメイン1: 現状実装」「ドメイン2: 拡張仕様」「ドメイン3:
  領域外」「判定フロー」「判定が難しいケース」の5サブセクションが存在する
- 配置位置が「## デザイン品質の具体ルール」と「## 人間の視点での最終検証」の
  間である

### 3-11. 変更1-G: 「iPad改善の採用チェックリスト」#1 修正

#### 変更概要

現状の「iPad改善の採用チェックリスト」セクションには、複数のチェック項目が
記載されている。その #1（最初のチェック項目）が v2 世代の「直射日光下視認性
が第一優先」という前提で書かれているため、v3 世代の「対策装備で解決済み・
第一優先ではなくなりつつある」表現に更新する。

#### 変更前文言

実ファイル確認済み。現状の「## iPad改善の採用チェックリスト」セクション内の
最初のチェック項目（#1）の原文を Claude Code が GitHub MCP または `git show`
で取得すること。

参考として、現状の #1 は概ね以下の趣旨である:

- 「直射日光下での視認性が確保されているか」を第一優先のチェック項目として
  記述している

このチェック項目を以下の「変更後文言」に置換する。**他のチェック項目（#2 以降）
は一切変更しない**。

#### 変更後文言

#1 のチェック項目本文を以下に置換する。

```markdown
### #1. 直射日光下視認性は対策装備で解決済みであることを前提とする

直射日光下視認性は、最高級アンチグレアフィルム + 開放カバー + 遮光日傘 +
熱対策の装備セットで解決済みである。そのため UI 改善の採用判断において
「直射日光下視認性」を第一優先のチェック項目として扱わない。

ただし対策リソースは一定残す:

- 装備が万全でない状況（出先での試合・装備忘れ等）でも最低限の視認性を
  確保する
- 装備による解決と UI による解決の役割分担を明確に保つ（UI 側で過剰に
  視認性に振らない）
- 装備が将来的に変更される可能性を考慮し、極端な視認性軽視は避ける

採用判定の優先軸は v3 世代では以下の通り:

- 第一: その画面で最も求められる要素 × 高いデザイン性の共存
  （GameScreen=視認性×デザイン、Stats=分析性×デザイン 等）
- 第二: 質感アイデンティティとの整合
- 第三: 色の意味の重複化禁止の遵守
- 第四: 装備が万全でない状況での最低限の視認性
```

#### Edit 実行方針

- 変更前文言（#1 のチェック項目原文）が GitHub MCP で取得した実ファイルと
  完全一致することを確認
- `str_replace` で #1 のチェック項目のみを上記「変更後文言」で置換
- #2 以降のチェック項目は **絶対に変更しない**
- 「## iPad改善の採用チェックリスト」見出し自体も変更しない

##### 整合性確認の追加要件

- #1 を変更する前に、#2 以降のチェック項目本文を実ファイルから確認する
- #2 以降に「直射日光下視認性が第一優先」という v2 世代の前提を反映した
  記述が含まれていないかをチェックする
- 含まれている場合、#1 だけを更新すると #2 以降との整合が取れなくなるため、
  独自判断で進めず **太一さんに確認を求める**
- 確認内容は「#2 以降の該当箇所原文 + 整合性が崩れる懸念点」を明記して
  報告する

#### 完了確認

- #1 のチェック項目本文が新記述に置き換わっている
- #2 以降のチェック項目が変更前と完全一致している
- セクション全体の構造（チェック項目の数・順序）が変更前と同じ

### 3-12. 第3章 完了条件

以下を全て満たした時点で作業1（molkky-ui-design/SKILL.md の改修）完了とする。

#### 構造の確認

- [ ] 改修後の `molkky-ui-design/SKILL.md` が以下12セクション構成になっている

  1. 運用背景（修正済み）
  2. 機能とデザインの両立思想（新規）
  3. 画面別の設計方針（全面置換、7画面記述）
  4. 情報ブロック（維持）
  5. 視線誘導の原則(維持)
  6. 色の意味の重複化禁止（新規）
  7. frontend-design プラグインとの関係（全面置換）
  8. AI生成っぽいUI回避チェックリスト（維持）
  9. iPad改善の採用チェックリスト（#1 のみ修正）
  10. デザイン品質の具体ルール（維持）
  11. 三層ドメイン分類（新規）
  12. 人間の視点での最終検証（維持）

- [ ] セクションの並び順が上記の通りである
- [ ] 維持セクション（情報ブロック / 視線誘導の原則 / AI生成っぽいUI回避
      チェックリスト / デザイン品質の具体ルール / 人間の視点での最終検証）の
      本文が変更前と1文字単位で完全一致している

#### 内容の確認

- [ ] 1-A: 運用背景セクションが本指示書記載の「変更後文言」と完全一致
- [ ] 1-B: 画面別の設計方針セクションが7画面構成（GameScreen/Stats/
      SetupScreen/Settings/WindMonitor/試合結果画面/試合カード）になっている
- [ ] 1-C: 機能とデザインの両立思想セクションが本指示書記載の「変更後文言」と
      完全一致
- [ ] 1-D: 色の意味の重複化禁止セクションが本指示書記載の「変更後文言」と
      完全一致
- [ ] 1-E: frontend-design プラグインとの関係セクションが本指示書記載の
      「変更後文言」と完全一致
- [ ] 1-F: 三層ドメイン分類セクションが本指示書記載の「変更後文言」と完全一致
- [ ] 1-G: iPad改善の採用チェックリスト #1 が本指示書記載の「変更後文言」と
      完全一致、#2 以降は変更前と完全一致

#### ビルド・検証

- [ ] 第3章完了直後に `npm run build` を実行し、ビルドエラーがないことを確認
      （UI 設計ドキュメントの変更のみだが、念のためビルド確認は実施する）
- [ ] 改修内容を1コミットにまとめてコミット
      （第7章のコミット粒度規則に従う）

#### 報告事項

- [ ] 第3章完了時点で、以下を太一さんに報告
  - 各サブセクション（1-A 〜 1-G）の Edit 実行結果
  - 変更後ファイルサイズ
  - 想定外の問題があった場合はその内容と対応

---

## 第4章: 作業2 — molkky-spec-writer/SKILL.md の改修

### 4-1. 変更対象ファイル情報

- パス: `.claude/skills/molkky-spec-writer/SKILL.md`
- 確度: 実ファイル確認済み（GitHub MCP, SHA: `1af5d73792eae28d620510e91b431212cb006263`）
- 変更前ファイルサイズ: 約 3 KB
- 変更後ファイルサイズ: 約 6-7 KB

### 4-2. 改修方針

1新規 + 1拡張。既存セクションへのテキスト変更はゼロ。

| サブセクション | 種別 | 配置位置 |
|---|---|---|
| 2-A: 指示書作成前のチェック | 新規 | 大原則の直後 |
| 2-B: よくある失敗パターンの回避 拡張 | 拡張 | 既存3項目 + 新規9項目 = 12項目 |

### 4-3. 変更2-A: 「指示書作成前のチェック」新規追加

新規セクションの全文は以下（v3-part2.md の対応箇所と同一）:

```markdown
## 指示書作成前のチェック

指示書を書き始める前に、触る対象の正典化強度・三層ドメイン分類を確認する。
判断基準の詳細は `molkky-ui-design/SKILL.md` の「画面別の設計方針」
「三層ドメイン分類」セクションを参照すること。

このチェックを怠ると、Claude Code が「触ってよい領域」と
「改善エビデンスが必須の領域」を区別できず、既存資産が
意図せず改変されるリスクがある。

### Step 1: 三層ドメイン判定（触るかどうかの一次判定）

触る対象が以下のいずれに該当するかを最初に判定する。

- **現状実装**: 既に正典化済みの確定領域
  → 触らないか、触るなら厳格な根拠が必要
- **拡張仕様**: 新機能受け入れ枠
  → 受け入れ可能、DESIGN.md 該当章への追記で対応
- **領域外**: 本アプリのスコープ外
  → そもそも触らない（基本不採用）

### Step 2: 正典化強度判定（触る場合の根拠強度）

触ることが決まった場合、対象画面の正典化強度に応じて
指示書に含めるべき情報の濃度を変える。

#### 濃い強度（GameScreen 風速W除く / WindMonitor）

- 指示書の背景セクションに **改善エビデンス項目を必須記載**
- エビデンスの内容:
  - 実機検証結果（iPad Pro M5 で確認した具体的な問題点）
  - ユーザー観察（試合中に発生した実害）
  - なぜ既存実装が不十分かの根拠
- PR本文にも同じエビデンスを記載するよう指示書末尾で要求する
  （`molkky-code-review` スキル第10章と連動）

#### 中強度（試合カード / 試合結果画面 / 設定画面 / SetupScreen / Stats方針）

- 指示書の背景セクションに **改善理由を明記**
- コミットメッセージに改善理由を含めるよう Claude Code に指示する

#### 薄い強度（Stats実装 / GameScreen 風速W）

- 通常運用、特別な追加要求なし
- 段階的分割と検証手順は通常通り適用

### Step 3: 新機能の場合の暫定強度

新機能は最初**正典化強度=薄い**でスタートする扱いを指示書冒頭に記載。
数試合運用して問題なければ強度を上げて確定する旨も併記する。
これにより Claude Code 側でも「初回実装は確定版ではない」と
理解した上で実装してもらえる。

### チェック実施タイミング

- 指示書のドラフト作成**前**（=最初）に実施する
- 「背景・目的」セクションを書く時点ではすでに判定済みの状態にする
- 判定結果は指示書本文に明示しなくてよいが、
  Claude AI（指示書作成者）の判断根拠としては必須
```

### 4-4. 変更2-B: 「よくある失敗パターンの回避」拡張

#### 改修方針の再掲

既存3項目を維持し、新規9項目を追加して合計12項目にする。
新規9項目は、過去の指示書運用で実際に発生した失敗事例・暗黙知化していた
教訓を言語化したものである。

#### 配置位置

- 既存の「よくある失敗パターンの回避」セクション内に、3つの既存項目の後に
  9項目を追加する形で実装する
- セクションの冒頭リード文（既存）は維持する
- 既存3項目の本文・順序は **絶対に変更しない**

#### 変更後文言（新規9項目本文・既存3項目の直後に追加）

```markdown
### 4. 位置指定の3点セット

指示書内で「変更対象箇所」を指定する際は、以下3点を併記する。

- 節番号（指示書内のセクション番号、例: §3-5）
- 実見出しテキスト（変更対象ファイル内の見出しを原文ママ、例: "## 運用背景"）
- 行番号（指示書作成時点の行番号、例: 行42-58）

1点だけ（節番号のみ・行番号のみ）だと、編集中に行番号がずれた時や見出しが
変更された時に齟齬が生じる。3点併記により、いずれか1点が変動しても残りの
2点で位置を一意化できる。

### 5. 変更前文言の完全一致確認

各 Edit 実行前に、指示書記載の「変更前文言」と実ファイルの該当箇所が
**1文字単位で完全一致** するかを確認する。

- 確認は GitHub MCP または `git show` 等で実ファイルから取得した原文と
  比較する
- 末尾改行・空白・全角半角・改行コードの差異も検査する
- 不一致を発見した場合、独自判断で Edit を進めず、太一さんに確認を求める
- 「微妙な差異だから合わせて Edit する」判断は禁止（指示書側の誤りなのか
  実ファイル側の差異なのかを Claude Code 単独で判定できない）

過去事例: 「不自然な否定文が実装直前に検出」事故。指示書の変更前文言が
実ファイルと微妙に異なっており、Edit 実行段階で初めて発覚した。

### 6. 基準SHA固定禁止

指示書内に基準SHA（main の SHA、変更対象ファイルの blob SHA 等）の
固定値を埋め込まない。代わりに作業開始時に Claude Code に動的確認させる。

- 指示書には `git rev-parse HEAD` 等のコマンドを記載する
- 確認した SHA は作業ログ・PR本文に記録する
- 指示書に固定値を埋め込むと、指示書作成時点と実装時点で SHA が変わった場合に
  「前提が壊れている」状態を検出できない

過去事例: 基準SHA固定で前提SHA衝突事故。指示書に固定 SHA を書いたまま、
実装時点で main が進んでおり、変更前文言の不一致が想定外の箇所で発生した。

### 7. 段階的ビルド検証（最低3回）

大規模変更（複数ファイル変更・複数サブタスク分割）では、作業途中で複数回
`npm run build` を実施する。

- タイミングは指示書側で明示する（例: 作業1完了直後・作業3完了直後・
  PR作成前）
- 最終ビルドだけだと、エラー発生箇所の特定が困難になる
- 大規模指示書では「途中で失敗 → 全体やり直し」が致命的なため、段階的
  検証で早期にエラー検出する

過去事例: 「途中失敗で全体やり直しが致命的」教訓。10ファイル分割の指示書で
最終ビルドだけ実施し、途中の作業3が原因のエラーが最後まで発覚せず、
作業4以降の修正コストが膨大になった。

### 8. Edit戦略: replace_all 禁止

`str_replace` を使う際、同一テキストが複数箇所に存在する場合でも
`replace_all` は使用しない。

- 各 Edit は **前後文脈を含めて一意化** された個別 Edit とする
- 同一テキストが複数箇所にある場合、それぞれの前後文脈を含めて識別する
- `replace_all` は「意図せぬ箇所まで変更される」リスクがあるため、
  指示書側でも禁止を明示する

替わりに、各箇所を「前後数行を含む文脈で一意化した old_str」で個別 Edit する。
これにより、Claude Code が変更箇所を取り違えるリスクを排除する。

### 9. コミット粒度の論理的分割

1コミット = 1論理単位の粒度を守る。論理単位の定義は指示書側で明示する。

- ファイル単位（1コミット = 1ファイル）が適切な場合
- 機能単位（1コミット = 1機能、複数ファイルに渡る）が適切な場合
- 領域単位（1コミット = 1領域、例: A領域→B領域→C領域 の3コミット構造）が
  適切な場合

指示書には「本タスクではどの粒度を採用するか」を明記し、Claude Code 側で
迷わない構造とする。粒度を明示しないと、Claude Code が独自判断で全変更を
1コミットにまとめてしまう、または逆に細分化しすぎる、といった事態が起きる。

### 10. コミットメッセージへの枝文脈併記

大規模指示書から派生したコミットでは、コミットメッセージに **枝文脈** を併記する。

- 例: "feat: GameScreen WindWidget pill実装（PR β §7.2 Buttons全面実装）"
- 枝文脈の書式: `（指示書名 §節番号 サブタスク名）`
- 後から `git log` で「このコミットがどの指示書のどのサブタスクから派生したか」を
  辿れるようにする

特に複数 PR から並行で派生したコミットが混在する場合、枝文脈なしでは
履歴の追跡が困難になる。

### 11. 大規模指示書の境界維持パターン

10ファイル分割等の大規模指示書では、各ファイル間の境界を明示する
チェーン構造を採用する。

- 各ファイル末尾に「次のファイルへ」のリンク・参照を記載
- 各ファイル冒頭に「前のファイルから引き継ぐ前提」を記載
- 途中で失敗した場合に、当該ファイルから再開可能な構造とする

書式テンプレートは現状未定。本タスク（4ファイル変更）では大規模分割は
不要だが、将来的に10ファイル超の指示書を作成する際は本項目に従う。

### 12. GitHub MCP 経由の実ファイル確認

GitHub MCP が利用可能な環境では、記憶ベースの引用ではなく、GitHub MCP
経由で実ファイルから取得した内容を優先する。

- 指示書作成時、変更対象ファイルの現状記述は GitHub MCP で取得する
- 取得した SHA は「確認時刻のスナップショット」として指示書に記載する
- 記憶ベースの引用は「ラベル体系・命名規則を実ファイルと取り違える」リスクが
  あるため、確実な実ファイル確認に置き換える

過去事例: 2026-04-19、known-pitfalls.md に存在しない 1-a〜1-d ラベル体系を
前提に指示書を作成した事案。記憶ベース記述は実害を生む。

並列作業時（複数チャット・複数 Claude AI が同じファイルを同時参照する場合）、
重複確認による無駄を避けるため、確認担当の分担を運用ルール側で整理する
（本項目では Claude Code 単独作業を前提とするため、分担問題は対象外）。
```

#### Edit 実行方針

- 既存3項目本文の末尾と、新規9項目の冒頭を連結する形で `str_replace` を実行
- 既存3項目（番号1〜3）の本文・順序は変更しない
- 新規9項目は番号4〜12として連番を維持
- 「## よくある失敗パターンの回避」見出し自体は変更しない

#### 完了確認

- 「## よくある失敗パターンの回避」セクション内に12項目（既存3 + 新規9）が
  存在する
- 既存3項目の本文・順序が変更前と1文字単位で完全一致
- 新規9項目の本文が本指示書記載の「変更後文言」と完全一致
- 各項目が番号付き見出し（### 1. 〜 ### 12.）で構造化されている

### 4-5. 第4章 完了条件

- [ ] 改修後の `molkky-spec-writer/SKILL.md` が以下の構造になっている
  1. 指示書の大原則（維持）
  2. 指示書作成前のチェック（新規）
  3. 指示書のテンプレート構造（維持）
  4. バグ報告の書き方（BEERフレームワーク）（維持）
  5. UI変更指示の書き方（維持）
  6. 計画→承認→実装フロー（維持）
  7. よくある失敗パターンの回避（拡張: 既存3項目 + 新規9項目 = 12項目）
- [ ] 既存セクション（1, 3, 4, 5, 6）の本文が変更前と1文字単位で完全一致
- [ ] 新規セクション「指示書作成前のチェック」が大原則の直後に配置
- [ ] よくある失敗パターンの回避が12項目構成

---

## 第5章: 作業3 — molkky-code-review/SKILL.md の改修

### 5-1. 変更対象ファイル情報

- パス: `.claude/skills/molkky-code-review/SKILL.md`
- 確度: 実ファイル確認済み（GitHub MCP, SHA: `30231623fd9710bb56dc40205375d2145009c75c`）
- 変更前ファイルサイズ: 約 3 KB
- 変更後ファイルサイズ: 約 4-5 KB

### 5-2. 改修方針

第10章新設のみ。既存第1〜9章は完全維持（テキスト変更ゼロ）。

### 5-3. 変更3-A: 第10章「正典化強度に応じた追加検証」新規追加

#### 改修方針の再掲

`molkky-code-review/SKILL.md` に第10章を新設する。既存第1〜9章は
完全維持（テキスト変更ゼロ）。第10章は spec-writer 側「指示書作成前の
チェック」と対称構造を取り、書く前と書いた後の両方で防御線を張る設計。

#### 配置位置

- 既存第9章の末尾の直後、ファイル末尾に追加する
- 既存第1〜9章の本文・見出し・順序は **絶対に変更しない**

#### 変更後文言（第10章全文）

```markdown
## 第10章: 正典化強度に応じた追加検証

PR 作成前のセルフレビューにおいて、変更対象画面・領域の **正典化強度** に
応じた追加検証を実施する。本章は spec-writer 側「指示書作成前のチェック」と
対称構造を取り、「書いた後の防御線」を担う。

### 10-1. 正典化強度マップ（参照）

正典化強度の現行マップは `.claude/skills/molkky-ui-design/SKILL.md` の
「画面別の設計方針」セクションに記載されている。本章では強度値を直接
記載せず、ui-design 側のマップを参照する形にする（二重メンテを避ける
ため）。

検証時は以下の手順で強度を確認する。

1. 変更対象の画面・領域を特定する
2. ui-design/SKILL.md の「画面別の設計方針」で当該画面の強度を確認
3. 強度に応じて 10-2 〜 10-4 のいずれかの追加検証を実施

ui-design 側のマップが更新された場合、本章は自動的に追従する（参照のみ
のため）。

### 10-2. 濃い領域に変更を加えた場合（必須）

変更対象画面の正典化強度が「濃い」の場合、PR本文に **改善エビデンス**
が3要素揃って記載されているかを検証する。

#### 必須3要素

1. **実機検証結果**: iPad Pro M5 等の実機で確認した具体的な問題点
2. **ユーザー観察**: 試合中・運用中に発生した実害（誰が・いつ・どう困ったか）
3. **既存実装が不十分である根拠**: 現行実装で解決できない理由

#### 検証手順

- PR本文を読み、上記3要素がすべて記載されているかを確認
- 1つでも欠ける場合、レビューを差し戻す（マージブロック相当）
- 3要素すべて揃っていても、内容が「実機検証ではなく推測」「ユーザー観察
  ではなく仮説」等の場合は、補強を求める

#### 該当画面（参考）

- GameScreen 本体（GameWindWidget を除く）
- WindMonitor

### 10-3. 中強度領域に変更を加えた場合

変更対象画面の正典化強度が「中強度」の場合、PR本文または直近の
コミットメッセージに **改善理由** が明記されているかを確認する。

#### 確認事項

- 改善理由が1〜2行でも明記されているか
- 改善理由が「なんとなく良くしたい」等の曖昧表現になっていないか
- 既存実装の「何が不十分か」が言語化されているか

改善理由がない・曖昧な場合、レビューコメントで質問する（マージブロック
ではなく、補足要請）。

#### 該当画面（参考）

- 試合カード
- 試合結果画面
- Settings
- SetupScreen
- Stats（方針 = 中強度）

### 10-4. 薄い領域に変更を加えた場合

変更対象画面の正典化強度が「薄い」の場合、通常レビュー基準で進める。

- エビデンス・改善理由は必須ではない
- ただしテスト・型チェック・ビルド検証等の機械的検証は通常通り実施
- AI 生成っぽい UI 回避チェックリスト（ui-design 側）等の標準チェックは
  従来通り適用

#### 該当画面（参考）

- Stats（具体実装）
- GameScreen の GameWindWidget（pill型風速計）

### 10-5. 三層ドメイン分類との連動

正典化強度は、三層ドメイン分類（ui-design 側）における **ドメイン1（現状実装）**
内での判定軸である。

- ドメイン1（現状実装）への変更: 強度判定が必須（10-2 〜 10-4 を適用）
- ドメイン2（拡張仕様）への新規追加: 暫定強度=薄い から始まる（10-4 適用、
  ただし 10-6 の暫定強度ルールも参照）
- ドメイン3（領域外）の変更: 基本不採用（PR 自体がレビュー対象外）

PR 作成前にドメイン分類を確認し、ドメイン1 への変更時は強度判定を、
ドメイン2 への新規追加時は暫定強度の明示を、それぞれ PR本文に記載する。

### 10-6. 新機能の暫定強度ルール

新機能（ドメイン2: 拡張仕様）として実装された変更は、最初は **暫定強度=薄い**
で扱う。

- 初回 PR では 10-4（薄い領域の検証）のみ適用
- 数試合運用して問題なければ、強度を中強度〜濃いに昇格させて確定する
- 暫定強度から確定強度への昇格は **太一さんの判断** で決まる
- 昇格判断は ui-design/SKILL.md「画面別の設計方針」の更新と連動する

#### 検証時の注意

- 新機能 PR では「これは暫定強度=薄い扱い」と PR本文に明示することを推奨
- 暫定強度のまま長期運用される場合、確定昇格の機会を逸する可能性がある
  （Claude Code 側からは昇格を提案できるが、最終判断は太一さん）

### 10-7. 強度判定が困難なケース

以下のケースでは、Claude Code 単独で強度判定をせず、太一さんに確認を求める。

- ui-design 側のマップに該当画面の記載がない（マップ更新漏れの可能性）
- 複数画面に跨る変更で、画面ごとに強度が異なる
- 「ドメイン1 と 2 の境界」で判定が割れる（既存画面に新要素を追加する等）

確認時は「該当画面・該当変更内容・判定が割れる理由」を明記して報告する。
```

#### Edit 実行方針

- `str_replace` で既存第9章末尾の直後に第10章を追加挿入する
- old_str に既存第9章末尾数行を含めて文脈を一意化
- new_str に「第9章末尾の数行 + 改行 + 上記第10章全文」を指定
- 既存第1〜9章の本文・見出し・順序は **絶対に変更しない**

#### 完了確認

- 既存第1〜9章の本文が変更前と1文字単位で完全一致
- 第10章が末尾に追加されている
- 第10章内に7サブセクション（10-1 〜 10-7）が存在する
  - 10-1: 正典化強度マップ（参照）
  - 10-2: 濃い領域に変更を加えた場合（必須）
  - 10-3: 中強度領域に変更を加えた場合
  - 10-4: 薄い領域に変更を加えた場合
  - 10-5: 三層ドメイン分類との連動
  - 10-6: 新機能の暫定強度ルール
  - 10-7: 強度判定が困難なケース

### 5-4. 第5章 完了条件

- [ ] 既存第1〜9章の本文が変更前と1文字単位で完全一致
- [ ] 第10章「正典化強度に応じた追加検証」が末尾に追加されている
- [ ] 第10章内に以下のサブセクションが存在
  - 正典化強度マップ（参照）
  - 濃い領域に変更を加えた場合（必須）
  - 中強度領域に変更を加えた場合
  - 薄い領域に変更を加えた場合
  - 三層ドメイン分類との連動
  - 新機能の暫定強度ルール

---

## 第6章: 作業4 — .claude/rules/ui-design.md のポインタ化

### 6-1. 変更対象ファイル情報

- パス: `.claude/rules/ui-design.md`
- 確度: 実ファイル確認済み（GitHub MCP）
- 変更前ファイルサイズ: 1901 B
- 変更後ファイルサイズ: 約780 B
- 変更種別: ファイル全文置換

### 6-2. 改修方針

詳細版（`.claude/skills/molkky-ui-design/SKILL.md`）への一元化を実施する。
過去のサマリ版が SKILL.md と部分的に重複・一部矛盾していたため、
本ファイルは情報の二重メンテを避けるためポインタ化する。

既存内容は **全文削除** し、ポインタ専用ファイルに置換する。

### 6-3. 変更4-A: ファイル全文置換

#### 変更前文言（全文）

以下が `.claude/rules/ui-design.md` の現状全文である。確度: 実ファイル確認済み
（GitHub MCP, SHA: `09f0fbb8f4b9d0801c588ce5e9ca9a866c872b51`）。

```markdown
# UI設計ルール（UI v2）

## 画面別優先度

| 画面 | 最優先 | 二次 | 三次 |
|------|--------|------|------|
| GameScreen | 視認性 | 操作性 | 美観 |
| Stats | 美観 | 情報量 | 操作性 |
| Settings | 操作性 | わかりやすさ | 美観 |

## コア設計原則

1. **情報ブロックは凝集単位。** チーム名 + プレイヤー名 + スコア + ミスドットは1つのまとまりとして扱う。分割配置しない。
2. **チーム固有色は廃止。** アクティブ/非アクティブの2状態のみで区別。
3. **色の役割は厳密に分離。** ブランド色 / アクション色 / 状態色 / 警告色 — 1つの色が複数の意味を持たない。
4. **視覚階層の軸は layout > position > size。** 色は補助的手段。
5. **背景は半明色。** ダークネイビーはヘッダーとアクティブ強調に限定。

## 禁止事項

- **過装飾禁止。** 情報ブロックの装飾過剰が最大リスク。
- **AI-generic UI禁止。** テンプレートからではなく文脈から設計する。
- **透明背景は「存在しない」扱い。** カジノキャラクター等の透明背景要素は無視。
- **入力ボタンサイズは isTablet ロジック上限を超えない。**

## 品質チェック

コード実装後に確認：
- 実データ（長いテキスト、0件、エッジ値）でテストしたか？
- 1つの支配的要素に意図的な階層コントラストがあるか？
- 色数は最小限か？（追加するより削る）
- iPad直射日光下で視認可能か？
- iPhoneでもレイアウトが安定しているか？

## モック画像作成時の注意

動的サイズのレンダリングは、コードを実行して実際のサイズを確認する（Trump cardフォントサイズの教訓：想定より遥かに小さくなった）。
```

#### 変更後文言（全文）

以下の内容で全文置換する。

```markdown
# UI設計ルール

UI設計の詳細は `.claude/skills/molkky-ui-design/SKILL.md` を参照すること。

このファイルは過去のサマリ版として存在していたが、詳細版（SKILL.md）への
一元化を実施した。情報の二重メンテを避けるため、本ファイルはポインタとして
残置する。

## 主要な参照先（SKILL.md内のセクション名）

- 機能とデザインの両立思想
- 画面別の設計方針（7画面）
- 情報ブロック
- 視線誘導の原則
- 色の意味の重複化禁止
- frontend-design プラグインとの関係
- AI生成っぽいUI回避チェックリスト
- iPad改善の採用チェックリスト
- デザイン品質の具体ルール
- 三層ドメイン分類
- 人間の視点での最終検証

> **このファイルにルールを書き足さないこと。**
> UI設計のルール追加・変更は SKILL.md 側で行う。
> 本ファイルへの追記は二重メンテの再発を招く。
```

#### Edit 実行方針

本ファイルは全文置換のため、`str_replace` でファイル内容全体を一致させる
方式で実装する。手順:

1. ファイルの現在の内容と本指示書記載の「変更前文言」が **1文字単位で完全
   一致** であることを確認する
2. 一致した場合のみ、`str_replace` の `old_str` に変更前文言全文、`new_str` に
   変更後文言全文を指定して実行する
3. 一致しない場合は Edit を実行せず、太一さんに確認を求める

### 6-4. 完了条件

以下を全て満たした時点で作業4完了とする。

- [ ] `.claude/rules/ui-design.md` の内容が本指示書記載の「変更後文言」と
      1文字単位で完全一致している
- [ ] ファイルサイズが約780 B である（誤差は許容）
- [ ] ファイル末尾の改行が変更前と同一の状態である（GitHub上で末尾改行の
      有無が一致）
- [ ] 当該変更が単独のコミットとして記録されている（次章の第7章でコミット
      メッセージ規則を指定）

---

## 第7章: 検証・コミット・PR作成

### 7-1. 段階的ビルド検証

最低3回の `npm run build` を実施する。

| タイミング | 検証目的 |
|---|---|
| 第3章（作業1: molkky-ui-design）完了直後 | 最大規模変更後の早期エラー検出 |
| 第5章（作業3: molkky-code-review）完了直後 | 中盤の検証ポイント |
| 第6章（作業4: ui-design.md ポインタ化）完了直後・PR作成前 | 全変更の最終ビルド確認 |

ビルドエラーが発生した場合、当該作業の Edit 内容を見直し、原因を特定してから
次の作業に進む。

### 7-2. コミット粒度（4コミット構造）

1ファイル = 1コミットの粒度で4コミットに分割。

```
コミット1: feat: molkky-ui-design SKILL.md 12セクション化（v3整合）
コミット2: feat: molkky-spec-writer SKILL.md 指示書作成前のチェック追加
コミット3: feat: molkky-code-review SKILL.md 第10章追加
コミット4: refactor: .claude/rules/ui-design.md ポインタ化
```

### 7-3. コミットメッセージ命名規則

- prefix: `feat:` または `refactor:`
  - 新規セクション追加・既存セクションの全面置換 → `feat:`
  - ポインタ化のような構造変更 → `refactor:`
- 対象ファイル名（拡張子なし）を明記
- 変更の論理単位を明示
- 必要に応じて枝文脈（v3整合等）を併記

### 7-4. PR作成手順

1. 全コミットをプッシュ
2. PR作成: base = main, head = feature/code_side_skills_canonization
3. PRタイトル: `feat: Code側スキル正典化（4ファイル統一改修, v3整合）`
4. PR本文に以下を含める（次項参照）

### 7-5. PR本文テンプレート

```markdown
## 概要

Claude Code 側の3スキル + 1ルールファイルを v3 世代の正典化された設計思想と
整合させる一括改修。

## 変更ファイル

- `.claude/skills/molkky-ui-design/SKILL.md`: 4新規 + 3修正、12セクション化
- `.claude/skills/molkky-spec-writer/SKILL.md`: 1新規 + 1拡張
- `.claude/skills/molkky-code-review/SKILL.md`: 第10章新設
- `.claude/rules/ui-design.md`: ポインタ化（全置換）

## 改修の背景

- v2世代の3画面記述（GameScreen/Stats/Settings）が現状（7画面）と乖離
- サマリ版（rules/ui-design.md）と詳細版（SKILL.md）の二重メンテで矛盾発生
- 「機能とデザインの両立」「色の意味の重複化禁止」「三層ドメイン分類」を
  明示的に文書化する必要

## 基準SHA（作業開始時）

[Claude Code が動的に確認した main の SHA を記載]

## ビルド検証結果

- 作業1完了直後: ✅ 成功
- 作業3完了直後: ✅ 成功
- 作業4完了直後: ✅ 成功

## 関連

- v2引き継ぎドキュメント: `aesthetic_canonization_handover_v2.md`
- v3引き継ぎドキュメント: `code_side_skills_canonization_handover_v3.md`

## 確認依頼

太一さんによる手動レビュー後、太一さんの判断でマージ。
Claude Code は自動マージを行わない。
```

---

## 第8章: 完了条件・確認事項

### 8-1. 全体完了条件チェックリスト

- [ ] 第3章完了条件をすべて満たしている
- [ ] 第4章完了条件をすべて満たしている
- [ ] 第5章完了条件をすべて満たしている
- [ ] 第6章完了条件をすべて満たしている
- [ ] 段階的ビルド検証(3回)がすべて成功している
- [ ] 4コミットの構造で履歴が記録されている
- [ ] PR が作成されている

### 8-2. 報告事項

- 各作業（1〜4）の Edit 実行結果のサマリ
- 変更後ファイルサイズの一覧
- 想定外の問題が発生した場合はその内容と対応
- PR の URL

### 8-3. PR は手動でレビュー（マージは太一さんが実施）

PR作成後、Claude Code は自動マージを行わない。太一さんによる手動レビュー・
マージを待つ。

---

## 指示書 ここまで

本指示書は太一さんによる手動 commit でリポジトリ root に配置される。
ファイル名: `code_side_skills_canonization_spec_v1.md`
